javascript:(function(){if(document.getElementById('gm-ui'))return;const d=document,b=d.body,c=e=>d.createElement(e);const ui=c('div');ui.id='gm-ui';ui.style='position:fixed;top:10px;right:10px;width:220px;background:#000;border:2px solid #0f0;color:#0f0;z-index:99999;font-family:monospace;padding:10px;max-height:90vh;overflow-y:auto';ui.innerHTML='<div style="display:flex;justify-content:space-between;margin-bottom:10px"><b>💀 GOD MODE</b><span id="gm-x" style="cursor:pointer">[X]</span></div>';b.appendChild(ui);d.getElementById('gm-x').onclick=()=>ui.remove();const add=(t,f)=>{const btn=c('button');btn.innerText='> '+t;btn.style='display:block;width:100%;background:#222;color:#fff;border:1px solid #0f0;margin-bottom:5px;cursor:pointer;text-align:left;padding:5px';btn.onclick=f;ui.appendChild(btn)};add('📸 IG: DL Buttons',()=>{if(!location.host.includes('instagram'))return alert('Go to Instagram!');d.querySelectorAll('img').forEach(i=>{if(i.width<200||i.parentNode.querySelector('.gdl'))return;const x=c('a');x.className='gdl';x.innerHTML='⬇';x.href=i.src;x.target='_blank';x.style='position:absolute;top:5px;right:5px;z-index:99;background:#0f0;color:#000;padding:2px 6px;text-decoration:none;font-weight:bold';i.parentNode.style.position='relative';i.parentNode.appendChild(x)})});add('📸 IG: Get Video URL',()=>{const r=performance.getEntriesByType('resource').filter(e=>e.name.includes('.mp4'));r.length?window.open(r[0].name):alert('Play a video first!')});add('📘 FB: Auto Add (Slow)',()=>{if(!location.host.includes('facebook'))return alert('Go to Facebook!');let max=prompt('How many?',10),cnt=0;const iv=setInterval(()=>{if(cnt>=max){clearInterval(iv);return alert('Done')}const btn=[...d.querySelectorAll('div[role="button"]')].find(e=>e.innerText=='Add friend');if(btn){btn.click();cnt++}else{window.scrollBy(0,500)}},2500)});add('🛠 Edit Page',()=>{d.designMode=d.designMode=='on'?'off':'on';alert('Edit Mode: '+d.designMode)});add('🛠 Zap Element',()=>{alert('Click to remove. ESC to stop.');const f=e=>{e.preventDefault();e.stopImmediatePropagation();e.target.remove()};d.addEventListener('click',f,true);d.addEventListener('keydown',e=>{if(e.key=='Escape'){d.removeEventListener('click',f,true);alert('Stopped')}},{once:true})});add('🛠 Remove Images',()=>{d.querySelectorAll('img').forEach(e=>e.remove())});add('🛠 Extract Links',()=>{const l=[...d.querySelectorAll('a')].map(a=>a.href).join('<br>');const w=window.open('','_blank');w.document.write('<style>body{background:#000;color:#0f0;font-family:monospace}</style>'+l)});})();
